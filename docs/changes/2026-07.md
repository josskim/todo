## 2026-07-02 - 할 일 핀 고정

### 요청 원문

> todo 할일 중 상단에 핀고정 할수있도록해줘. 핀 고정된건 항상 제일 위에 있도록.

### 결과 요약

- `todo_todos`에 `isPinned` 컬럼을 추가해 할 일별 핀 고정 상태를 저장하도록 했다.
- 할 일 카드에 `핀 고정` / `고정 해제` 버튼과 고정 배지를 추가했다.
- 검색/필터/정렬 상태와 관계없이 핀 고정된 할 일이 목록 최상단에 먼저 나오도록 정렬 로직을 변경했다.
- 서버 조회 기본 정렬도 `isPinned desc`, `createdAt desc` 순서로 맞췄다.
- Prisma 전체 `db push`는 공유 DB의 다른 테이블 삭제 경고가 있어 중단했고, 필요한 컬럼/인덱스만 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`와 `CREATE INDEX IF NOT EXISTS`로 안전하게 반영했다.

### 변경 파일

- `prisma/schema.prisma`
- `prisma/todo-init.sql`
- `src/app/(app)/todos/page.tsx`
- `src/app/actions/todo-actions.ts`
- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-07.md`

### 검증

- `ALTER TABLE "todo_todos" ADD COLUMN IF NOT EXISTS "isPinned" boolean NOT NULL DEFAULT false` 적용 확인
- `CREATE INDEX IF NOT EXISTS "todo_todos_isPinned_idx" ON "todo_todos" ("isPinned")` 적용
- `npm.cmd run build` 성공
- `git diff --check` 통과

### 커밋

- `7c79000` Add pinned todos
## 2026-07-02 - 핀 고정 버튼 아이콘 정리

### 요청 원문

> todo 프로젝트 "고정해제" 옆에 "고정" 아이콘은 필요 없을듯.

### 결과 요약

- 핀 고정되지 않은 카드의 `고정` 버튼에서 아이콘을 제거하고 텍스트만 남겼다.
- 기존 `고정 해제` 버튼과 핀 고정 동작, 정렬 우선순위는 그대로 유지했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-07.md`

### 검증

- `npm.cmd run build` 성공
- `git diff --check` 통과

### 커밋

- `pending`

## 2026-07-02 - todo Neon DB 분리

### 요청 원문

> todo, calendar, staynamcheon을 Neon 안에서 todo 프로젝트 만들어 뒀어. todo만 따로 분리해줘. 혹시 모르니 DB전체 백업하고 복구 가능하도록 한뒤에 분리작업해줘. calendar, staynamcheon은 같은 스테이남천에서 사용하는거라. 그냥 같이 가는게 맞을듯.

### 결과 요약

- 기존 공유 Neon DB 전체를 custom dump와 schema dump로 백업했다.
- 새 Neon todo DB에 todo Prisma 스키마를 생성했다.
- todo 전용 테이블 9개만 새 DB로 복원했다.
- 원본 공유 DB의 todo 테이블은 롤백용으로 삭제하지 않고 유지했다.
- 로컬 `todo/.env.local`은 새 todo DB를 바라보도록 변경했다. 환경파일은 Git 추적 대상이 아니며, 연결 문자열은 문서에 기록하지 않았다.

### 백업

- 전체 DB 백업: `E:\dev\backups\neon-shared\20260702-231405\shared-neon-full.dump`
- 전체 DB 스키마 백업: `E:\dev\backups\neon-shared\20260702-231405\shared-neon-schema.sql`
- 복구 목록 검증: `E:\dev\backups\neon-shared\20260702-231405\shared-neon-full.list`
- todo 테이블별 데이터 백업: `E:\dev\backups\todo-migration\20260702-231630`

### 변경 파일

- `docs/changes/2026-07.md`
- `.env.local` 변경, Git 미추적 파일

### 검증

- `pg_dump --format=custom` 전체 백업 생성
- `pg_restore --list`로 전체 백업 목록 검증
- `prisma db push --url <new todo db>`로 새 DB 스키마 생성
- todo 테이블 row count 비교
  - `todo_users`: 4 -> 4
  - `todo_categories`: 11 -> 11
  - `todo_tags`: 2 -> 2
  - `todo_todos`: 48 -> 48
  - `todo_todo_tags`: 29 -> 29
  - `todo_history`: 141 -> 141
  - `todo_reminders`: 7 -> 7
  - `todo_notifications`: 327 -> 327
  - `todo_push_subscriptions`: 3 -> 3

### 커밋

- `9857e88` Document todo Neon database split

## 2026-07-02 - 핀 고정 배지 제거

### 요청 원문

> 아까 5.4로 todo 프로젝트 "고정해제" 옆에 "고정" 아이콘은 필요 없을듯.이거 수정했는데.. 아직 수정이 안되어있어. 확인해서 수정해줘.

### 결과 요약

- 핀 고정된 카드 상단 우측에 별도로 보이던 `고정` 배지를 제거했다.
- 핀 고정 상태에서는 액션 버튼의 `고정 해제`만 남도록 정리했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-07.md`

### 검증

- `git diff --check` 통과
- `npm.cmd run build` 통과

### 커밋

- $hash Remove pinned todo badge

## 2026-07-03 - PC 알림 시간 입력 피커 보강

### 요청 원문

> todo PC에서는 알림시간이 클릭(세팅) 안되는거 같아. 모바일에서는 되는듯. 확인해줘

### 결과 요약

- 공통 `Input` 컴포넌트를 `forwardRef` 기반으로 바꾸고, `date`, `time`, `datetime-local` 입력에서는 클릭 시 네이티브 `showPicker()`를 호출하도록 보강했다.
- 데스크톱 브라우저에서 날짜/시간 입력칸을 눌렀을 때 바로 피커가 열리도록 `cursor-pointer` 스타일도 함께 적용했다.
- 모바일 동작은 유지하면서, PC에서 "클릭은 되는데 세팅창이 안 뜨는" 체감을 줄이는 방향으로 정리했다.

### 변경 파일

- `src/components/ui.tsx`
- `docs/changes/2026-07.md`

### 검증

- `npm.cmd run lint` 통과
- `npm.cmd run build` 통과

### 커밋

- `9ed45fd` Improve todo datetime-local picker

## 2026-07-03 - PC 날짜 선택기 개선

### 요청 원문

> PC에서도 연도-월-일 부분 클릭해도 달력 띄워주고,
> 오른쪽 달력 아이콘 다크모드에서 블랙인데.. 다크모드에서 화이트로 되도록 해줘

### 결과 요약

- `date`, `time`, `datetime-local` 입력에서 마우스 다운 시 `showPicker()`를 열어 PC에서 본문을 눌러도 달력이 바로 뜨도록 개선했다.
- 네이티브 캘린더 아이콘은 숨기고, 입력칸 안에 공통 아이콘을 직접 렌더링해서 다크모드에서는 흰색, 라이트모드에서는 어두운색으로 보이게 정리했다.
- 입력 오른쪽 여백도 늘려서 아이콘이 텍스트와 겹치지 않게 맞췄다.

### 변경 파일

- `src/components/ui.tsx`
- `src/app/globals.css`
- `docs/changes/2026-07.md`

### 검증

- `npm.cmd run lint` 통과
- `npm.cmd run build` 통과

### 커밋

- `3e42ad4` Improve todo date picker UI

## 2026-07-06 - 할 일 작성/수정 textarea 표시 깨짐 수정

### 요청 원문

> todo 글 작성  및 수정시.. textarea 박스 깨지고있어.
> 글 내용은 textarea 밖으로 나가고, 커서는 textarea 안에 있고.. 전반적으로 확인해서 수정해줘.

### 결과 요약

- 할 일 작성/수정 모달의 `title` 입력 textarea가 투명 입력창과 표시용 preview 레이어를 겹쳐 쓰는 구조라, 긴 내용이 preview 레이어 밖으로 삐져나오던 문제를 수정했다.
- preview 레이어를 textarea wrapper 안에서 `overflow-hidden`으로 클리핑하고, textarea 스크롤 시 preview 레이어의 `scrollTop`을 동기화했다.
- textarea와 preview의 `line-height`를 맞추고 최소 높이를 지정해 커서 위치와 표시 텍스트 위치가 어긋나지 않도록 정리했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-07.md`

### 검증

- `cmd /c npm run build` 성공

### 커밋

- `a160c06` Fix todo textarea preview overflow
## 2026-07-07 - todo textarea 스크롤 및 처리 표시 안정화

### 요청 원문

> 위에 첨부파일 3개 참조해서 아래내용진행해줘. 혹시 첨부파일 안보이면 얘기해주고,
>
> 1,2번 첨부 파일은 마지막줄 입력시.. 스크롤이 제일 아래로 내려오지 않아서. 글자가 안보임. 2번째 이미지 같이 스크롤 해서 내리면 마지막줄이 보임.
>
> 3번째 첨부파일은 드래그해서 실선처리/해재시 적용후 제일 위에 글 위치로 이동됨.
> 마지막줄 드래그 안됨. 실선처리된 영역과 안된 영역을 같이 드래그해서 실천처리하면 뭐가 꼬임. 적용이 되든 안되든 뭐라도 되어야하는데 꼬여서 뭐라도 적용이 잘안됨.

### 결과 요약

- 할 일 작성/수정 textarea에서 마지막 줄 입력 시 caret 위치가 보이도록 textarea와 처리 표시 미리보기 레이어의 스크롤을 동기화했다.
- 하단 resize 영역과 마지막 줄이 겹치지 않도록 textarea와 미리보기 레이어의 하단 여백을 맞췄다.
- 처리 표시/해제 버튼 클릭 시 선택 영역과 스크롤 위치가 위로 튀지 않도록 보존했다.
- 이미 처리된 영역과 미처리 영역을 함께 드래그해도 중첩 `~~` 마커가 생기지 않도록 선택 영역 내부 마커를 정리한 뒤 처리/해제를 적용하도록 수정했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-07.md`

### 검증

- `cmd /c npm run build` 통과

### 커밋

- `1708f7a` Stabilize todo textarea processing

## 2026-07-07 - todo 할일 입력 1000자 확장

### 요청 원문

> todo 할일 수정하는데.. 아래 내용넣고 수정하면 에러나는거 같아.
> todo 할일 작성 textarea 글자수 현재 400 자로 표시되는데 1000 자 까지 입력할수있도록 해줘.

### 결과 요약

- 할 일 작성/수정 textarea의 최대 글자 수 표시와 클라이언트 제출 전 검증을 400자에서 1000자로 확장했다.
- 서버 zod 검증도 같은 `TODO_TITLE_MAX_LENGTH` 상수를 사용하므로 1000자까지 저장 가능하도록 함께 확장됐다.
- 실제 에러 원인이 될 수 있던 DB 컬럼 `todo_todos.title varchar(200)`을 `varchar(1000)`으로 확장했다.
- 긴 제목이 리마인더 알림으로 생성될 때 추가 에러가 나지 않도록 `todo_notifications.title`도 `varchar(1100)`으로 확장했다.
- Prisma schema와 초기 생성 SQL도 현재 DB 구조와 맞게 갱신했다.

### 변경 파일

- `src/lib/todo-limits.ts`
- `prisma/schema.prisma`
- `prisma/todo-init.sql`
- `docs/changes/2026-07.md`

### 검증

- DB 확인: `todo_todos.title` 최대 1000자, `todo_notifications.title` 최대 1100자 반영
- `cmd /c npm run build` 성공
- `cmd /c npm run lint` 성공, 기존 `todo-dashboard.tsx` hook dependency warning 1개 확인

### 커밋

- 예정
