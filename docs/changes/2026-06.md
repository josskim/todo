## 2026-06-28 - 할일 부분 처리 표시

### 요청 원문

> todo 에서 할일 수정시.. 
> 할일이 여러개 있을때..처리한 내용은 드래그해서  처리한것같이 글 내용중에 처리한것을 표시할수있도록 버튼 만들어서. 표시하게 해줘. 
> 첨부이미지 같이.
>
> 그리고 작업한거 모두 푸시해줘.

### 결과 요약

- 할 일 추가/수정 모달의 할일 입력 textarea에서 드래그 선택한 텍스트를 `처리 표시` 버튼으로 취소선 처리할 수 있게 했다.
- 선택 구간은 `~~선택 내용~~` 형태로 저장하고, 목록 화면에서는 해당 구간만 취소선으로 렌더링한다.
- 공용 `Textarea` 컴포넌트가 selection range 제어를 위해 ref를 받을 수 있도록 보강했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `src/components/ui.tsx`
- `docs/changes/2026-06.md`

### 검증

- `npm.cmd run build` 성공

### 커밋

- `30ead4f` Add partial completion marking for todos

## 2026-06-28 - 할일 처리 표시 즉시 반영 및 해제

### 요청 원문

> 처리표시 하니까.. 수정 완료하니 처리 표시는 되는데.. 처리표시 클릭시 바로 표시 되도록 해주고, 해제도 가능하도록 해줘.
> 푸시해줘

### 결과 요약

- 할 일 추가/수정 모달의 할일 입력창에서 선택 영역에 처리 표시를 적용하면 저장 전에도 즉시 취소선으로 보이도록 했다.
- 이미 `~~처리 표시~~`가 적용된 선택 영역이나 처리 표시 내부 텍스트를 다시 선택해 누르면 표시가 해제되도록 토글 동작을 추가했다.

### 변경 파일

- `src/components/todo-dashboard.tsx`
- `docs/changes/2026-06.md`

### 검증

- `npm.cmd run build` 성공

### 커밋

- `f2fc89a` Toggle todo partial completion marks
